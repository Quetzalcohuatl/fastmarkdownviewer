# Results

Status: **not yet measured**.

No startup number is published until 30 fresh-process ETW runs per variant and fixture are complete on the recorded Windows 11 x64 machine. This avoids turning development-machine noise or the harness's visible-window proxy into a product claim.

## Decision before measurement

egui/Glow remains the provisional implementation. The Direct2D prototype currently has release-blocking parity gaps for selectable text/accessibility, RaTeX composition, image policy, tables, and virtualized layout; therefore it cannot pass the selection rule regardless of an unverified launch-time advantage.
