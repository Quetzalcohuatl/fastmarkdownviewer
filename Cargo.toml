[package]
name = "fast-markdown-viewer"
version = "0.2.3"
edition = "2024"
rust-version = "1.95"
description = "A fast, read-only Markdown viewer"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Quetzalcohuatl/fastmarkdownviewer"
homepage = "https://Quetzalcohuatl.github.io/fastmarkdownviewer/"
readme = "README.md"
build = "build.rs"

[features]
default = ["renderer-glow"]
renderer-glow = ["eframe/glow"]
renderer-wgpu = ["eframe/wgpu"]

[[bin]]
name = "FastMarkdownViewer"
path = "src/main.rs"

[dependencies]
eframe = { version = "0.36.0", default-features = false, features = ["accesskit", "default_fonts"] }
# FMV registers its bounded loaders; commonmark's loader features are redundant.
egui_commonmark = { version = "0.25.0", default-features = false, features = ["pulldown_cmark"] }
image = { version = "0.25.10", default-features = false, features = ["gif", "jpeg", "png", "webp"] }
open = "5.3.2"
regex = "1.12"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
egui_commonmark_backend = { version = "0.25.0", default-features = false }
ratex-layout = "0.1.14"
ratex-parser = "0.1.14"
ratex-svg = { version = "0.1.14", features = ["embed-fonts"] }
ratex-types = "0.1.14"
resvg = { version = "0.48.1", default-features = false, features = ["text"] }
rusty-mermaid = { version = "=0.2.0", default-features = false, features = ["svg"] }
tempfile = "3.23.0"
rfd = { version = "0.15.4", default-features = false }
ureq = { version = "2.12.1", default-features = false, features = ["tls"] }
url = "2.5.7"

[target.'cfg(target_os = "linux")'.dependencies]
eframe = { version = "0.36.0", default-features = false, features = ["x11", "wayland"] }
rfd = { version = "0.15.4", default-features = false, features = ["xdg-portal", "async-std"] }

[target.'cfg(target_os = "macos")'.dependencies]
fmv-macos-events = { version = "0.1.0", path = "crates/macos-events" }

[build-dependencies]
winresource = "0.1.23"

[dev-dependencies]
ttf-parser = "0.25.1"

[profile.release]
opt-level = 3
# Full LTO shrinks the executable without the reading cost of opt-level s/z.
# Measurements: experiments/size/compiler-profiles.md.
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"

[patch.crates-io]
egui_commonmark = { path = "vendor/egui_commonmark" }
egui_commonmark_backend = { path = "vendor/egui_commonmark_backend" }

rusty-mermaid-diagrams = { path = "experiments/mermaid-spike/vendor/rusty-mermaid-diagrams" }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
